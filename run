apt update
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt -y install nodejs
ufw allow 3333
ufw allow 443
ufw allow 80
git clone https://github.com/ranalims99/prox && cd prox
cp .env.example .env
npm install
sudo node server.js
